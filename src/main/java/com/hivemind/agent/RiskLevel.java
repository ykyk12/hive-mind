package com.hivemind.agent;

/**
 * 工具风险等级。
 * 分级不是为了防外人，是为了让"蜂巢自己"在无人看守时也不会做出不可逆的事——
 * 低风险（读、算、查）可直接执行；中高风险必须带审批令牌。
 */
public enum RiskLevel {
    /** 只读、无副作用：直接执行 */
    LOW,
    /** 有外部副作用但可逆：需要审批令牌 */
    MEDIUM,
    /** 不可逆或涉及真实设备/系统：需要审批令牌 + 建议人工确认 */
    HIGH
}
